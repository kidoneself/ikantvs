# 项目进度 · jyinshi-next

> 更新时间：2026-07-26
> 运维方式：广州唯一后端（`api.naspt.vip`）+ 香港网关静态（`ikantvs.com`）；发版见 [`部署架构.md`](部署架构.md)。
> 产品决策基线：[`产品决策-2026-06-30.md`](产品决策-2026-06-30.md)
> 架构规范：[`.cursor/rules/architecture.mdc`](../.cursor/rules/architecture.mdc)

---

## 产品定稿（不再做）

| 项 | 结论 |
|---|---|
| 前台用户 | ❌ 无，全员游客 |
| 注册 / 登录 / 个人中心 | ❌ 不做 |
| 收藏 / 追剧 | ❌ 不做 |
| 会员 / 卡密 / 付费墙 | ❌ 不做（表 V022 已删） |
| 变现方式 | 老站同款：**网盘转存 + 推广分成**（夸克/百度为主） |

---

## 已完成

### 地基 & 部署

- [x] 单一仓库骨架 + `.gitignore`
- [x] Docker Compose 本地依赖（MySQL + Redis）
- [x] 数据库迁移 V001–V038
- [x] 后端 Spring Boot 健康检查 `/api/health`
- [x] 后端 Dockerfile + `docker-compose.prod.yml`（MySQL + Redis + 全栈）
- [x] 整站部署文件：`web/admin Dockerfile`、`Caddyfile.prod`
- [x] 生产发布脚本 `deploy-prod.sh`、已上线 ikantvs.com

### identity 域（仅运营账号）

- [x] `user` 表 + 角色（contributor / reviewer / admin）
- [x] 后台 JWT 登录 `/api/auth/login`、`/api/auth/me`
- [x] 后台账号管理（列表、改角色、封禁）
- [x] 无前台注册/登录

### content 域

- [x] `media` 表 + TMDB/豆瓣元数据采集（import / manual / refresh）
- [x] `media_link` 表 + 前台详情读链（approved、广告过滤、敏感词过滤）
- [x] `media_season` 季信息
- [x] 榜单 `ranking` / `ranking_item` + 前台展示 + 后台运营
- [x] 前台：首页信息流、站内搜索、详情、榜单
- [x] 海报 R2 镜像 + thumb
- [x] 站内搜索 Redis 缓存
- [x] 后台：影视库、隐藏列表、TMDB 补录/编辑、榜单管理、**链接管理**
- [x] **聚合采集 `content/ingest`**：PanSou / Gying 源 + 调度器，自动产候选链（替代原设计的独立 search 域）
- [x] **失效治理 `invalid_share`**（V036）+ 失效标记
- [x] **热度回写 job**：`content_event` 行为分（带衰减）叠加到 `media.hot_seed` → `media.hot`（V038 + `HotScoreService`）
- [x] 老库 `search_cache` → `media_link` 一次性迁移脚本（已跑完）
- [x] **每日更新 `content/daily`**（V039/V041）：策展看板，选剧 + 录入上游大佬源链并指定监控号；
  自营链 `media_link(source='self')` 详情页置顶；「更新至第 X 集/日期」智能提取（移植老站 `EpisodeExtractor`），
  支持**运营手动纠正集数**（`manual_episode`，与自动值取较新 = 保护手动 + 只增不减）

### transfer 域

- [x] 转存执行、夸克/百度/迅雷 client、转存记录、限流清理
- [x] 追更监控（`transfer_monitor`）；由每日更新驱动，首转/追更均回填最新集数
- [x] 网盘执行引擎合并进主站 `transfer.pan.*`（进程内 `PanJobRunner` 领任务执行），放弃独立 worker（V045 删 `transfer_worker` 表）
- [x] 平台网盘账号管理 + 登录态/凭据（cookie 加密存储，V028–V035）；账号分工 `role`（transfer/monitor，V040）
- [x] 详情页「获取链接 → 转存 → 返回推广链」（前台 `executeTransfer`）
- [x] 后台「转存/追更」页（任务队列 / 网盘账号；追更监控入口已移入「每日更新」按剧查看）

### ops 域

- [x] `sys_config` 系统配置（后台可改，含迅雷 SDK 开关）
- [x] `sensitive_word` 敏感词（拦截/转审核/打码/标记）
- [x] 前台公开配置 `/api/site/config`

### analytics 域

- [x] `content_event` 匿名埋点（搜索 / 详情浏览 / 链点击 / 卡片点击）
- [x] 已移除 `user_id`、收藏、付费墙事件（V022 + 代码）
- [x] **埋点去重**：`visitor_id`（前端 localStorage UUID）+ view 按「访客/片/天」Redis 去重（V037），刷新不再刷量
- [x] 前台上报 `/api/events`（含 `X-Visitor-Id` 头）
- [x] 后台数据洞察（热搜、求片榜、热门浏览、链点击榜）+ 概览近期活跃

### 其它

- [x] 产品决策文档 `docs/产品决策-2026-06-30.md`
- [x] `docs/产品需求文档.md` 标废弃（与现决策冲突，仅存历史参考）
- [x] README（域划分、部署方式、无会员说明）

---

## 未完成

### P1 · 内容运营

| 项 | 说明 |
|---|---|
| 审核台 | `media_link.status=pending` 通过/驳回/改匹配（后台菜单 `ready:false`，UI 未建） |
| 每日更新·前台 | 后台已完成；**前台板块未建**（应置首页最前、在观影/最新资源之前，按最近更新倒序） |
| ~~每日更新·worker~~ | ✅ 已完成：sync 本就回传 `latestFileName`；首转 first_save 现也回传（worker 成功后列落地夹挑最新文件名，`SaveResult.latestFileName`），主站据此自动回填集数 |
| 广告过滤规则 | 老站 `ad_filter_rule` 未迁，仍靠硬编码 `MediaLinkAdFilter`，后台不可配 |
| 录入贴链 | 影视库编辑时给 media 加 manual 链（现只能改元数据 + 链接管理页贴链） |

### P2 · 体验 & 配置

| 项 | 说明 |
|---|---|
| transfer 后台配置 | 转存开关/路径等 sys_config 未接入 ops |
| 详情链分层 | manual 主显 / pansou 折叠（PRD 有设计，前台未做） |
| 本地全栈 compose | 本地 compose 仅 mysql/redis，web/admin 仍要 `pnpm dev` |

### P2 · 站点运营配置（公告 / 加群 / 公众号 / 通知）

> 原则：**全部后台可配**，换群二维码 / 公众号 / 公告不用改代码发版。配置进 `sys_config`（ops），公开接口 `/api/site/config`。先别做交互式飞书机器人。

| 模块 | 状态 | 说明 |
|---|---|---|
| **网站公告** | ✅ 已做 | `site.notice.*`；前台顶栏条 + 弹窗 HTML；`show-once` 按内容 hash 记 localStorage |
| **运维通知** | ✅ 已做 | 默认老站飞书应用私聊（app-id/secret/open_id）；也可 `feishu_bot`/企微 webhook；1h 去重 |
| **加群 + 公众号 + 活码** | ✅ 已做 | 后台「活码/加群」双码上传；站内弹窗+悬浮钮；`/qr` 与 `naspt.vip/qr` 接新站（老投放码不用换） |

后台入口：系统配置 →「网站公告」「运维通知」。

### P3 · 多域名网盘策略

> **一套前后端**，按访问域名决定开哪些网盘；**不要**再维护老 Java 站两套逻辑。

| 项 | 状态 | 说明 |
|---|---|---|
| 域名网盘配置表 + 后台增删改 | ✅ 已做（待发版） | `site_domain_config`；后台「运营中心 → 域名网盘」 |
| `/api/site/config` + 搜链按域名过滤 | ✅ 已做（待发版） | Origin → Referer → Host；SSE 异步前解析 |
| 种子 | ✅ | `naspt.vip`=迅雷+磁力；`ik.naspt.vip`/`ikantvs.com`=全开 |
| 老站切流下线 | ⬜ | 先在 ik / ikantvs 验证，再把 `naspt.vip` 指到 next，最后停老 Java |

全局 `pan.display.*` 仍用于：**采集** + **未配置域名的回落**。

---

## 推荐下一步

```
1. 发版域名网盘 → 用 ik.naspt.vip / ikantvs.com 验证
2. 验证通过后切 naspt.vip → 停老 Java
3. 审核台 / ad_filter / transfer 后台配置（运营增强）
```

---

## 域包现状一览

| 域 | 包路径 | 状态 |
|---|---|---|
| common | `com.jyinshi.common` | ✅ 基础设施 |
| identity | `com.jyinshi.identity` | ✅ 仅运营账号 |
| content | `com.jyinshi.content` | ✅ 主链路 + ingest 采集 + 热度回写 + 每日更新看板 |
| transfer | `com.jyinshi.transfer` | ✅ 转存/追更/账号已上线（追更由每日更新驱动） |
| ops | `com.jyinshi.ops` | ⚠️ 部分（缺 ad_filter、transfer 配置） |
| analytics | `com.jyinshi.analytics` | ✅ 匿名埋点 + 去重 + 热度分 |
| search | — | ➖ 由 `content/ingest` 承担，不单独建域 |
| membership / billing | — | ❌ 已取消 |

---

## 变更记录

| 日期 | 内容 |
|---|---|
| 2026-06-30 | 初版：对齐产品决策，删用户/会员/收藏；埋点纯匿名；补整站部署文件 |
| 2026-06-30 | 新增独立 VPS 部署：`docker-compose.prod.yml`、`deploy-prod.sh`；目标机 103.24.216.84 |
| 2026-07-01 | 清理老项目复用遗留；运维改为纯线上，README 以发版为主 |
| 2026-07-04 | 校准至真实状态：transfer 域 / ingest 采集 / 链接管理 / 失效治理 已完成；新增埋点 visitor_id 去重（V037）+ 热度回写 job（V038）；`产品需求文档.md` 标废弃 |
| 2026-07-05 | 每日更新看板上线：`content/daily`（V039）+ 账号分工 `role`（V040）+ 手动集数 `manual_episode`（V041）；自营链详情页置顶；集数提取移植老站；追更监控从转存页移入每日更新按剧查看、可手动纠正集数（保护手动+只增不减）；首转也回填最新集数 |
| 2026-07-05 | 每日更新看板对齐运营诉求（V043）：区分「上次更新」(`last_content_at` 真更新)与「上次检查」(`last_probe_at`)，修正原「更新时间」每次巡检都变的误导；每部剧可设追更节奏 `check_days/check_hours/check_interval`（到点才查，`due()` 优先按每剧节奏，空则沿用全局）；看板重排为 更新至/上次更新(相对时间+今日绿点)/状态(追更中·源失效·暂停)/节奏；新增按剧「立即检查」`POST /admin/daily/{id}/check`；砍掉转存次数/文件数/文件日志（运营用不上） |
| 2026-07-05 | transfer-worker 首转回传最新集数：`SaveResult` 加 `latestFileName`，`JobExecutor.save(collectLatest=true)` 首转成功后经 `IncrementalSyncService.latestInFolder` 列落地夹挑最新（复用追更同一套挑选逻辑）；用户点转存传 false 不额外列夹、保持低延迟。至此首转/追更均自动落地集数 |
| 2026-07-13 | **合并 transfer-worker 进主站**（放弃「海外主站 + 国内 worker 分离」）：网盘引擎 driver/exec/account 平移到 `com.jyinshi.transfer.pan.*`，进程内 `PanJobRunner` 直接领任务执行、`PanAccountLoader` 从 `transfer_account` 加载账号、`PanLoginBridge` 就地落号；删除 `WorkerGatewayController`/`WorkerWakeupService`/`TransferWorker` 及一众 Worker*/Agent* DTO 与 `/api/worker/**` 放行（V045 删 `transfer_worker` 表）；迅雷应用级配置（client-id/secret/device-id/redirect-uri）移入 `sys_config`「迅雷转存」后台可改；后台「转存/追更」页去掉 worker 节点与加号弹框的 worker 选择 |
| 2026-07-26 | 前台短剧专区上线（`/drama`）；记入 **P3 最后做**：一套 next 按域名开盘——`naspt.vip` 仅磁力+迅雷，`ikantvs.com` / `ik.naspt.vip` 全开；老 Java 站切流后下线 |
| 2026-07-26 | **P2 公告 + 运维通知落地**：`site.notice.*` 前台顶栏/弹窗；`notify.*` 飞书/企微 webhook（`WebhookNotifyPort`）；加群/公众号二维码仍待做 |
| 2026-07-26 | **P3 域名网盘（代码已就位、未发版）**：`site_domain_config` + 后台增删域名/各盘开关；`/api/site/config` 与搜链按 Origin 过滤；种子 naspt=迅雷+磁力，ik/ikantvs=全开 |

## 部署记录 · 103.24.216.84

| 项 | 状态 |
|---|---|
| 生产 compose | ✅ `deploy/docker-compose.prod.yml`（自建 MySQL + Redis + 全栈） |
| 部署脚本 | ✅ `deploy/deploy-prod.sh`（默认 `./deploy-prod.sh web` 只发前台） |
| SSH / 整站 | ✅ 已上线 ikantvs.com |
