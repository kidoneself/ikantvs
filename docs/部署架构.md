# 部署架构 · 海外单机（开源默认）

> 开源仓默认交付：**一台海外 VPS + 一个域名**，全栈 Docker 一键起。

## 拓扑

```
用户浏览器
    │
    ▼
Caddy（80/443，自动 HTTPS）
    ├── /          → web 前台
    ├── /api       → Spring Boot 后端
    └── :8080      → admin 后台（建议防火墙限制 IP）
         │
    MySQL + Redis +（可选）pansou 采集
```

建议配置：**2 核 4G+** 海外 VPS（香港 / 新加坡 / 美西等）。

## 快速部署

```bash
git clone https://github.com/kidoneself/ikantvs.git
cd ikantvs/deploy
./deploy-single.sh init
# 编辑 .env.single：SITE_DOMAIN / ACME_EMAIL / 密码 / JWT_SECRET
# DNS：A 记录指向 VPS；放行 80/443
./deploy-single.sh up
```

| 入口 | 地址 |
|------|------|
| 前台 | `https://你的域名` |
| API 健康检查 | `https://你的域名/api/health` |
| 后台 | `http://服务器IP:8080`（默认 `admin` / `admin123`，务必改密） |

空库走 `deploy/init/`（最终态 schema + 演示种子），**不要**重放 `migrations/` 历史文件。

## 数据模式

| 模式 | 配置 | 说明 |
|------|------|------|
| 本地演示（默认） | 不填云端 | 仅演示片；可自建 pansou 采集 |
| 对接云端 | `JYINSHI_CLOUD_*` | 片库/取链走运营方 API |

```bash
JYINSHI_CLOUD_ENABLED=true
JYINSHI_CLOUD_BASE_URL=https://api.example.com/api
JYINSHI_CLOUD_API_KEY=sk_xxx

# 纯云端、不自采集时可关 pansou 省内存：
ENABLE_PANSOU=false
INGEST_ENABLED=false
```

## 相关文件

- `docker-compose.single.yml` — 单机 compose
- `Caddyfile.single` — 反代与 HTTPS
- `.env.single.example` — 环境变量模板
- `deploy-single.sh` — init / up / sync 脚本

## 远程发版（可选）

```bash
export DEPLOY_SERVER=root@your-vps-ip
./deploy-single.sh all
```

## 免责声明

本软件仅提供信息流展示与网盘转存技术能力。**部署者须自行确保内容来源与运营的合法合规**，开发者不对第三方资源负责。
