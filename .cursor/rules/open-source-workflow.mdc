---
description: 本工作区专属开源仓 ikantvs（与自用仓分离）
alwaysApply: true
---

# 开源工作区 · ikantvs

本 Cursor 窗口 / 本目录 **永远只做开源**。

| | 仓 |
|--|----|
| **本窗口** | [kidoneself/ikantvs](https://github.com/kidoneself/ikantvs)（`origin`） |
| **自用（别的窗口/目录）** | [kidoneself/jyingshi](https://github.com/kidoneself/jyingshi) |

本地远程：`origin` = ikantvs；`jyingshi` 仅作偶发对照，**不要往 jyingshi 推自用生产改动**（自用另开窗口）。

## 1. 分支

| 分支 | 用途 |
|------|------|
| **`main`** | **默认且唯一工作分支**。开源壳、对外文档、部署模板 |
| **`feature/*`** | 大改可选，合并回 `main` |

```bash
git checkout main
# 改完
git push origin main
```

**不要**在本窗口默认切到 `dev` 做自用运维。用户若提广州/香港/生产自用 → 提醒去自用仓窗口，本仓不改。

## 2. 提交前缀

| 前缀 | 含义 |
|------|------|
| **`pub:`** | 开源功能、部署模板、云端对接、对外文档（本仓默认） |
| **无前缀** | 小修 bug / 重构亦可 |

本仓 **不用** `priv:`（自用提交只存在于 jyingshi）。

## 3. 可改路径（开源区）

- `server/`、`web/`、`admin/`（不含硬编码生产域名/IP）
- `deploy/init/`、`deploy/deploy.sh`、`deploy/.env.example`、`deploy/docker-compose.yml`
- `deploy/Dockerfile.app`、`deploy/nginx-app.conf`、`deploy/entrypoint-app.sh`
- `.github/workflows/`、对外 README、LICENSE、开源向 `docs/`
- `common/cloud/` 云端对接

## 4. 禁止出现

- 真实 IP、域名（`naspt.vip`、`ikantvs.com`、`175.x`、`149.x` 等）
- API Key、JWT、R2/迅雷 secret、cookie
- 广州/香港/prod 自用 compose 与内部决策文档
- 硬编码账号密码（含测试代码）

默认值用 `example.com`，密钥走环境变量占位。

## 5. AI 约定

- **未说明** → 默认在 **`main`**，按开源处理，commit 用 **`pub:`**。
- 用户说「自用」「生产」「广州/香港」「jyingshi」→ **不在本窗口改代码**，提示换到自用项目。
- 改完告知当前分支，并提示是否 `git push origin main`。
