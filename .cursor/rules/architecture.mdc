---
description: jyinshi-next 架构与防乱铁律（所有后端代码都必须遵守）
alwaysApply: true
---

# jyinshi-next 后端架构铁律

本项目是「网盘影视资源聚合平台」。重做的首要目标是**不再变乱**。以下规则强制执行，
任何人（含 AI）写代码都必须遵守。设计全貌见 `docs/重做设计方案.md`。

## 1. 按业务域分包（最重要）

代码按**业务域**组织，每个域是 `com.jyinshi` 下的一个顶级包：

```
identity     用户：仅运营账号（后台登录/角色），无前台注册
content      内容：影视库、元数据采集、榜单、每日更新、聚合入库
search       搜索：前台 SSE 聚合搜索 + 文档资源发现
transfer     转存：网盘账号 + 一键转存 + 追更
ops          运营：敏感词、系统配置、域名网盘开关、活码
analytics    统计：匿名埋点、报表
common       跨域基础设施（仅限此处放通用代码；含云端数据源配置）
```
（membership / billing 已取消，不要再新建。）

- **铁律 1.1**：每个域**自带完整分层** `entity / mapper / dto / service / controller`（域内子包）。
- **铁律 1.2**：新增功能 = 在对应域里加，或新建一个域包。**严禁**新建全局的
  `controller/`、`service/` 大文件夹（老项目就是这么堆成 47+45 个的）。
- **铁律 1.3**：`common` 只放真正跨域的东西（统一返回、异常、安全、配置、工具）。
  **业务逻辑一律不许进 common。** 拿不准就放回它所属的域。

## 2. 分层职责

- `controller`：只做参数校验、调 service、包 `Result` 返回。**不写业务逻辑、不碰 Mapper。**
- `service`：业务逻辑唯一所在地。事务边界在这层。
- `mapper`：只做数据访问（MyBatis-Plus）。
- `dto`：对外的请求/响应对象。**实体 `entity` 不直接暴露给前端**，用 VO/DTO 转换。

## 3. 跨域调用

- 域之间**只能通过对方的 `service` 接口**调用，**不许跨域直接读对方的 Mapper / 表**。
- 例：其它域要读运营账号信息 → 调 `identity` 的 service，不许跨域直接查 `user` 表。

## 4. 改需求的铁律（防止「越改越乱」）

- **铁律 4.1**：需求变了，**改原处 + 删旧代码**。有 git 兜底，**严禁新旧并存**。
- **铁律 4.2**：**严禁**保留 `*.backup`、注释掉的整段旧逻辑、`XxxOld`、`XxxV2` 并存。
- **铁律 4.3**：每次改动保持**可编译、可运行**，不留半成品。

## 5. 统一约定

- 所有接口返回 `com.jyinshi.common.api.Result<T>`，分页用 `PageResult<T>`。
- 业务错误抛 `com.jyinshi.common.exception.BizException`，由全局异常处理器统一转 `Result`。
- 命名：类用大驼峰、表/字段用小写下划线、布尔配置开关语义化（如 `jyinshi.cloud.enabled`）。
- 开源壳对接上游数据时，只走 `jyinshi.cloud.*`（`JYINSHI_CLOUD_BASE_URL` + `API_KEY`），不要在各域散落硬编码上游地址。

## 6. 不要做的事

- 不把构建产物、数据导出（csv/tsv）、cookie、日志提交进仓库（见 `.gitignore`）。
- 不引入二道贩子做核心数据源（如元数据直连 TMDB 官方，不经第三方中转）。
- 不在一个 PR/一次改动里横跨多个域做大重构；一次只把一个域做到底。
