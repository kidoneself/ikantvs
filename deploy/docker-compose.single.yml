# 海外单机整站（一台 VPS）：MySQL + Redis + backend + web + admin + Caddy
# pansou 可选（profile=pansou）：自建采集才需要；纯云端数据可关掉省内存。
#
# 用法（deploy 目录，已配置 .env.single）：
#   ./deploy-single.sh up
# 或：
#   docker compose -f docker-compose.single.yml --env-file .env.single --profile pansou up -d --build
services:
  mysql:
    image: mysql:8.0
    container_name: jyinshi-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root123}
      MYSQL_DATABASE: jyinshi_next
      MYSQL_USER: ${DB_USERNAME:-jyinshi}
      MYSQL_PASSWORD: ${DB_PASSWORD:-jyinshi123}
      TZ: Asia/Shanghai
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/conf.d:/etc/mysql/conf.d:ro
      # 仅「空数据目录首次初始化」时执行最终态 schema + 演示种子；已有 volume 不受影响
      - ./init:/docker-entrypoint-initdb.d:ro
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --default-time-zone=+08:00
    networks:
      - jyinshi-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p${MYSQL_ROOT_PASSWORD:-root123}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  redis:
    image: redis:7-alpine
    container_name: jyinshi-redis
    restart: unless-stopped
    command: >
      redis-server
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
      --save 900 1
      --save 300 10
    networks:
      - jyinshi-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "2"

  pansou:
    profiles: ["pansou"]
    image: ghcr.io/fish2018/pansou-web:latest
    container_name: jyinshi-pansou
    restart: unless-stopped
    environment:
      CHANNELS: ${PANSOU_CHANNELS:-tgsearchers7,Aliyun_4K_Movies,bdbdndn11,yunpanx,bsbdbfjfjff,yp123pan,sbsbsnsqq,yunpanxunlei,tianyifc,BaiduCloudDisk,txtyzy,peccxinpd,gotopan,PanjClub,kkxlzy,baicaoZY,MCPH01,bdwpzhpd,ysxb48,jdjdn1111,yggpan,MCPH086,zaihuayun,Q66Share,Oscar_4Kmovies,ucwpzy,shareAliyun,alyp_1,dianyingshare,Quark_Movies,XiangxiuNBB,ydypzyfx,ucquark,xx123pan,yingshifenxiang123,zyfb123,tyypzhpd,tianyirigeng,cloudtianyi,hdhhd21,Lsp115,oneonefivewpfx,qixingzhenren,taoxgzy,Channel_Shares_115,tyysypzypd,vip115hot,wp123zy,yunpan139,yunpan189,yunpanuc,yydf_hzl,leoziyuan,pikpakpan,Q_dongman,yoyokuakeduanju}
      ENABLED_PLUGINS: ${PANSOU_PLUGINS:-labi,zhizhen,shandian,duoduo,muou}
      CACHE_TTL: ${PANSOU_CACHE_TTL:-60}
    volumes:
      - pansou_cache:/app/data/cache
    # 公网调试入口由 Caddy :8899（basic_auth）反代，不直曝容器端口
    mem_limit: 1g
    networks:
      - jyinshi-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1/api/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "2"

  backend:
    build:
      context: ../server
      dockerfile: Dockerfile
    image: jyinshi-next-backend:latest
    container_name: jyinshi-next-backend
    restart: unless-stopped
    env_file:
      - .env.single
    environment:
      TZ: Asia/Shanghai
      DB_URL: jdbc:mysql://mysql:3306/jyinshi_next?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
      DB_USERNAME: ${DB_USERNAME:-jyinshi}
      DB_PASSWORD: ${DB_PASSWORD:-jyinshi123}
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DATABASE: "0"
      CORS_ENFORCE_ORIGIN: ${CORS_ENFORCE_ORIGIN:-true}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      PANSOU_BASE: ${PANSOU_BASE:-http://pansou}
      XUNLEI_REDIRECT_URI: ${XUNLEI_REDIRECT_URI}
      JYINSHI_CLOUD_ENABLED: ${JYINSHI_CLOUD_ENABLED:-false}
      JYINSHI_CLOUD_BASE_URL: ${JYINSHI_CLOUD_BASE_URL:-}
      JYINSHI_CLOUD_API_KEY: ${JYINSHI_CLOUD_API_KEY:-}
      INGEST_ENABLED: ${INGEST_ENABLED:-true}
      INGEST_PANSOU_ENABLED: ${INGEST_PANSOU_ENABLED:-true}
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - jyinshi-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8888/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  web:
    build:
      context: ../web
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${VITE_API_BASE:-/api}
    image: jyinshi-next-web:latest
    container_name: jyinshi-next-web
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - jyinshi-net
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "2"

  admin:
    build:
      context: ../admin
      dockerfile: Dockerfile
    image: jyinshi-next-admin:latest
    container_name: jyinshi-next-admin
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - jyinshi-net
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "2"

  caddy:
    image: caddy:2-alpine
    container_name: jyinshi-next-caddy
    restart: unless-stopped
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN:?SITE_DOMAIN is required in .env.single}
      ACME_EMAIL: ${ACME_EMAIL:?ACME_EMAIL is required in .env.single}
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
      - "8899:8899"
    volumes:
      - ./Caddyfile.single:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend
      - web
      - admin
    networks:
      - jyinshi-net
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "2"

networks:
  jyinshi-net:
    driver: bridge

volumes:
  mysql_data:
  pansou_cache:
  caddy_data:
  caddy_config:
